"use client";
import Link from "next/link";import { FormEvent,useState } from "react";import { useRouter } from "next/navigation";import BrandLogo from "../BrandLogo";import styles from "./owner.module.css";
export default function OwnerLogin(){const router=useRouter(),[email,setEmail]=useState(""),[password,setPassword]=useState(""),[error,setError]=useState(""),[busy,setBusy]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError("");try{const r=await fetch("/api/owner/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,password})}),j=await r.json() as {error?:string};if(!r.ok)throw new Error(j.error??"Sign-in failed.");router.refresh()}catch(x){setError(x instanceof Error?x.message:"Sign-in failed.")}finally{setBusy(false)}}return <main className={styles.accessShell}><section className={styles.executiveLogin}><div className={styles.loginBrand}><BrandLogo kind="tradie" tone="dark" className={styles.accessLogo}/><span>Private owner environment</span></div><div className={styles.loginStatement}><p className={styles.eyebrow}>Executive access</p><h1>Owner<br/>Control Centre</h1><p>One protected view for projects, finance, people and decisions across Alert Tradie Pro.</p><div className={styles.loginSignals}><div><strong>Private</strong><span>Owner-only financial data</span></div><div><strong>Live</strong><span>Persistent projects and records</span></div></div></div><form className={styles.ownerLoginForm} onSubmit={submit}><div className={styles.loginFormHeading}><span>AM</span><div><strong>Registered owner</strong><small>Email and password required</small></div></div><label><span>Owner email</span><input type="email" autoComplete="username" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Enter your registered email" required/></label><label><span>Password</span><input type="password" autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Enter your password" required/></label>{error&&<p className={styles.loginError}>{error}</p>}<button className={styles.loginSubmit} type="submit" disabled={busy}>{busy?"Checking access…":"Enter Owner Control Centre"}<span>→</span></button><small>Protected by encrypted password verification, rate limiting and a private session.</small></form><Link className={styles.backToSite} href="/">← Return to public website</Link></section></main>}
